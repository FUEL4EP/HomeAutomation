#!/bin/sh

# generated by AsksinPP_addon_generator 0.93
# generated on Thu Apr 08 15:11:54 CEST 2021

# XML control file name  : HB-UNI-SENSOR-RAD-AL53_addon_control.xml

# creator                : FUEL4
# device name            : HB-UNI-SENSOR-RAD-AL53
# small case device name : hb-uni-sensor-RAD-AL53
# XML version            : 1.0
#------------------------------------------------------------------------------
# in der WebUI angezeigter Gerätetyp, muss identisch sein mit dem Firmware-XML-Tag: <type name="HB-UNI-SENSOR-RAD-AL53" id="HB-UNI-SENSOR-RAD-AL53">
DEVICE="HB-UNI-SENSOR-RAD-AL53"
# in der WebUI angezeigte Gerätebeschreibung
DEVICE_DESC="Radioaktivit\&auml;tssensor"
# Name der Piktogramme, bestehend aus xx.png bzw. xx_thumb.png
DEVICE_IMG=hb-uni-sensor-RAD-AL53.png
DEVICE_THUMB=hb-uni-sensor-RAD-AL53_thumb.png
FIRMWARE_FILE=/firmware/rftypes/hb-uni-sensor-RAD-AL53*.xml

#------------------------------------------------------------------------------
# Edit DEVDB.tcl
devdescrFile="/www/config/devdescr/DEVDB.tcl"
devdescrSearch="array[[:space:]]*set[[:space:]]*DEV_PATHS[[:space:]]*{"

devdescrInsert="$DEVICE {{50 \/config\/img\/devices\/50\/$DEVICE_THUMB} {250 \/config\/img\/devices\/250\/$DEVICE_IMG}} "

if [ -z "`cat $devdescrFile | grep \"$DEVICE\"`" ]; then
	sed -i "s/\($devdescrSearch\)/\1$devdescrInsert/g" $devdescrFile
fi

#------------------------------------------------------------------------------
# Edit webui.js
webuiFile="/www/webui/webui.js"
webuiSearch="DEV_HIGHLIGHT[[:space:]]*=[[:space:]]*new Array();"

webuiInsert="\n"
webuiInsert="${webuiInsert}DEV_HIGHLIGHT['$DEVICE'] = new Object();\n"
webuiInsert="${webuiInsert}DEV_LIST.push('$DEVICE');\n"
webuiInsert="${webuiInsert}DEV_DESCRIPTION['$DEVICE']='$DEVICE_DESC';\n"
webuiInsert="${webuiInsert}DEV_PATHS['$DEVICE'] = new Object();\n"
webuiInsert="${webuiInsert}DEV_PATHS['$DEVICE']['50'] = '\/config\/img\/devices\/50\/$DEVICE_THUMB';\n"
webuiInsert="${webuiInsert}DEV_PATHS['$DEVICE']['250'] = '\/config\/img\/devices\/250\/$DEVICE_IMG';"

if [ -z "`cat $webuiFile | grep \"$DEVICE\"`" ]; then
	sed -i "s/\($webuiSearch\)/\1$webuiInsert/g" $webuiFile
fi

# ab hier siehe Anleitung von Jérôme, siehe https://homematic-forum.de/forum/viewtopic.php?f=76&t=60067


webuiSearchBegin="elvST[[:space:]]*=[[:space:]]*new Array();"




### Edit webuiInsert ####

webuiFile="/www/webui/webui.js"
 

# datapoint name #1: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR
webuiInsertParam="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR"
webuiInsertValue="stringTableHBCOUNTS_PER_HOUR"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #2: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX
webuiInsertParam="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX"
webuiInsertValue="stringTableHBCOUNTS_PER_HOUR_MAX"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #3: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED
webuiInsertParam="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED"
webuiInsertValue="stringTableHBCOUNTS_PER_HOUR_FILTERED"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #4: HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE
webuiInsertParam="HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE"
webuiInsertValue="stringTableHBCOUNTS_PER_SAMPLE"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #5: HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE
webuiInsertParam="HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE"
webuiInsertValue="stringTableHBACCUMULATOR_VOLTAGE"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #6: HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE
webuiInsertParam="HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE"
webuiInsertValue="stringTableHBAL53_SUPPLY_VOLTAGE"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 
 

# datapoint name #7: HB_MEASUREMENT_RAD|OPERATING_VOLTAGE
webuiInsertParam="HB_MEASUREMENT_RAD|OPERATING_VOLTAGE"
webuiInsertValue="stringTableHBBatteryVoltage"
webuiInsert="\n"
webuiInsert="${webuiInsert}elvST['$webuiInsertParam'] = '\${$webuiInsertValue}';"
if [ -z "`cat $webuiFile | grep \"$webuiInsertParam\"`" ]; then
	sed -i "s/\($webuiSearchBegin\)/\1$webuiInsert/g" $webuiFile
fi
 
 



### Edit stringtable_de.txt ###

stringtable_deFile="/www/config/stringtable_de.txt"
 

# datapoint name #1: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR
 
stringtable_deInsert="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR\t\${stringTableHBCOUNTS_PER_HOUR}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|COUNTS_PER_HOUR\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #2: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX
 
stringtable_deInsert="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX\t\${stringTableHBCOUNTS_PER_HOUR_MAX}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #3: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED
 
stringtable_deInsert="HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED\t\${stringTableHBCOUNTS_PER_HOUR_FILTERED}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #4: HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE
 
stringtable_deInsert="HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE\t\${stringTableHBCOUNTS_PER_SAMPLE}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #5: HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE
 
stringtable_deInsert="HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE\t\${stringTableHBACCUMULATOR_VOLTAGE}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #6: HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE
 
stringtable_deInsert="HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE\t\${stringTableHBAL53_SUPPLY_VOLTAGE}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi
 

# datapoint name #7: HB_MEASUREMENT_RAD|OPERATING_VOLTAGE
 
stringtable_deInsert="HB_MEASUREMENT_RAD|OPERATING_VOLTAGE\t\${stringTableHBBatteryVoltage}"
if [ -z "`cat $stringtable_deFile | grep \"HB_MEASUREMENT_RAD|OPERATING_VOLTAGE\"`" ]; then
    echo -e $stringtable_deInsert >> $stringtable_deFile
fi



### Edit translate.lang.stringtable.js ###

translate_deFile="/www/webui/js/lang/de/translate.lang.stringtable.js"
translate_deSearch="\"dummy\" : \"\","

 

# datapoint name #1: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR
 
translate_deInsert="\n    \"stringTableHBCOUNTS_PER_HOUR\" : \"Z%E4hlimpulse%20pro%20h\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBCOUNTS_PER_HOUR\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #2: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_MAX
 
translate_deInsert="\n    \"stringTableHBCOUNTS_PER_HOUR_MAX\" : \"Z%E4hlimpulse%20pro%20h%20max\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBCOUNTS_PER_HOUR_MAX\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #3: HB_MEASUREMENT_RAD|COUNTS_PER_HOUR_FILTERED
 
translate_deInsert="\n    \"stringTableHBCOUNTS_PER_HOUR_FILTERED\" : \"Z%E4hlimpulse%20pro%20h%20filtered\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBCOUNTS_PER_HOUR_FILTERED\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #4: HB_MEASUREMENT_RAD|COUNTS_PER_SAMPLE
 
translate_deInsert="\n    \"stringTableHBCOUNTS_PER_SAMPLE\" : \"Z%E4hlimpulse\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBCOUNTS_PER_SAMPLE\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #5: HB_MEASUREMENT_RAD|ACCUMULATOR_VOLTAGE
 
translate_deInsert="\n    \"stringTableHBACCUMULATOR_VOLTAGE\" : \"Akkumulatorspannung%28ADC%29\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBACCUMULATOR_VOLTAGE\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #6: HB_MEASUREMENT_RAD|AL53_SUPPLY_VOLTAGE
 
translate_deInsert="\n    \"stringTableHBAL53_SUPPLY_VOLTAGE\" : \"AL53%20Betriebsspannung%20%28ADC%29\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBAL53_SUPPLY_VOLTAGE\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 
 

# datapoint name #7: HB_MEASUREMENT_RAD|OPERATING_VOLTAGE
 
translate_deInsert="\n    \"stringTableHBBatteryVoltage\" : \"Betriebsspannung\","
if [ -z "`cat $translate_deFile | grep \"stringTableHBBatteryVoltage\"`" ]; then
      sed -i "s/\($translate_deSearch\)/\1$translate_deInsert/g" $translate_deFile
fi
 


#finished 'AsksinPP addon script generator 0.93'
